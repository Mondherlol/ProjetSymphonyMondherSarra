{% extends 'base.html.twig' %}

{% block title %}Etudiant{% endblock %}

{% block body %}
<nav class="navbar navbar-expand-lg navbar-dark bg-dark" style="background-color:black!important">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">Gestion de stages</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor02" aria-controls="navbarColor02" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarColor02">
      <ul class="navbar-nav me-auto">
        <li class="nav-item">
          <a class="nav-link active" href="/enseignant">Enseignants
          </a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/soutenance">Soutenance</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/etudiant">Etudiant</a>
        </li>
       
      </form>
    </div>
  </div>
</nav>

    <h1>Etudiant</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ etudiant.id }}</td>
            </tr>
            <tr>
                <th>Nce</th>
                <td>{{ etudiant.nce }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ etudiant.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ etudiant.prenom }}</td>
            </tr>
            <tr>
                <th>Numéro soutenance</th>
                <td>{{ etudiant.soutenance.id }}</td>
            </tr>
        </tbody>
    </table>

    <a  class="btn-warning btn " href="{{ path('app_etudiant_index') }}">Retourner à la liste</a>

    <a  class="btn-info btn " href="{{ path('app_etudiant_edit', {'id': etudiant.id}) }}">Modifier</a>

    {{ include('etudiant/_delete_form.html.twig') }}
{% endblock %}
